server:
  host: "0.0.0.0"
  port: 8888
  ssl_enabled: true
  cert_path: "/path/to/your/fullchain.pem"
  key_path: "/path/to/your/privkey.pem"

auth:
  default_user: "root"
  default_pass: "root@0305"
  session_timeout: 30 # 分鐘

database:
  file_path: "secure_vault.db"
  backup_folder: "./backups"
  max_history: 20